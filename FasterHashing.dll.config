<configuration>
	<!-- 
		Change the target filename to the actual filename,
		or create a symlink from /lib/libssl.so to the
		versioned libssl.so
	-->

	<dllmap dll="libssl" target="libssl.so.1.0.0" />
</configuration>